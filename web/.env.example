# Monitoreo de errores (Sentry) del frontend - opcional, dejar vacio si no se usa
VITE_SENTRY_DSN=

# URL del backend en produccion (ej. https://campeonapp-api.onrender.com). Dejar
# vacio en desarrollo local (el proxy de Vite ya redirige /api al backend local).
VITE_API_URL=
